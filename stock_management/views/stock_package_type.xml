<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="ev_stock_package_type_form" model="ir.ui.view">
            <field name="name">ev_stock.package.form.editable</field>
            <field name="model">stock.package.type</field>
            <field name="inherit_id" ref="stock.stock_package_type_form"/>
            <field name="arch" type="xml">
                  <xpath expr="//label[@for='length_uom_name']" position="attributes">
                   <attribute name="string">Dimensions</attribute>
               </xpath>
                <xpath expr="//div[@name='size']" position="after">
                    <field name="size_id"/>
                </xpath>
                <field name="barcode" position="attributes">
                    <attribute name="invisible">1</attribute>
                </field>
                <field name="company_id" position="after">
                    <field name="product_id"/>
                    <field name="barcode"/>
                    <field name="package_type_barcode_ids" widget="one2many_list">
                        <list string="Barcodes">
                            <field name="name"/>
                        </list>
                    </field>
                </field>
            </field>
        </record>
    </data>
</odoo>
